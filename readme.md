
## Create Readme

You have to create a `Readme.md` file. and write down following questions. Dont Try to copy paste from AI Tools. Just write what you know about these. If you don't know , then search , learn , understand and then write.

### 6. Answer the following questions clearly:

1. What is the difference between **getElementById, getElementsByClassName, and querySelector / querySelectorAll**?
Ans: On JS DOM we can get elements with these key.
    getElementById- It will return single and unique element from HTML.

    getElementsByClassName- This will search specific Class and returns element from HTML.

    querySelectorAll- This is most static in JS DOM. That returns an NodeList of all matching elements.

2. How do you **create and insert a new element into the DOM**?
Ans: For creating any element or tag we can use document.getElement() method into the DOM, and if we try to insert a new element we can use append() method.

3. What is **Event Bubbling** and how does it work?
Ans: Event Bubbling is a type of event propagation where an event starts at the most specific element and then "bubbles" up through its ancestors in the DOM tree, all the way to the top. It starts at the bottom and moves upward.

4. What is **Event Delegation** in JavaScript? Why is it useful?
Ans: Event Delegation is a design pattern in JavaScript used to manage events efficiently. It's more easier than 'for of loops'.

5. What is the difference between **preventDefault() and stopPropagation()** methods?
Ans: I cannot understand how to write this answer.

---

